# Развертывание статической версии на demo.rokets.delivery

## 🎯 Цель

Запустить статическую версию классифайда на `demo.rokets.delivery` **БЕЗ Node.js App**, используя только веб-сервер (Apache/Nginx).

## ✅ Что уже готово

- ✅ Статический билд собран (папка `out/`)
- ✅ Папка `out/` добавлена в Git
- ✅ Все файлы готовы к развертыванию

## 📋 Пошаговая инструкция

### Шаг 1: Получить файлы на Verpex через Git

**Вариант A: Через Terminal в cPanel (рекомендуется)**

1. Откройте **cPanel → Terminal**
2. Выполните команды:

```bash
# Создать папку для поддомена (если нет)
mkdir -p ~/public_html/demo

# Перейти в папку
cd ~/public_html/demo

# Клонировать репозиторий во временную папку
cd ~
git clone https://github.com/ShavalievER/rokets-classifieds.git rokets-temp

# Скопировать файлы из out/ в папку поддомена
cp -r ~/rokets-temp/commerce/out/* ~/public_html/demo/
cp -r ~/rokets-temp/commerce/out/.* ~/public_html/demo/ 2>/dev/null || true

# Удалить временную папку
rm -rf ~/rokets-temp

# Проверить, что файлы скопированы
ls -la ~/public_html/demo/
```

Должны быть файлы: `index.html`, `404.html`, `_next/`, `account/`, `product/`, и т.д.

**Вариант B: Через Git Version Control в cPanel**

1. **cPanel → Git Version Control**
2. **Create** новый репозиторий:
   - **Repository URL**: `https://github.com/ShavalievER/rokets-classifieds.git`
   - **Repository Path**: `repositories/rokets-static`
   - **Repository Name**: `rokets-static`
3. После клонирования через Terminal:
   ```bash
   cp -r ~/repositories/rokets-static/commerce/out/* ~/public_html/demo/
   ```

### Шаг 2: Настроить поддомен в cPanel

1. **cPanel → Subdomains**
2. Найдите поддомен `demo` или создайте его:
   - **Subdomain**: `demo`
   - **Document Root**: `public_html/demo` (или `public_html/subdomains/demo`)
   - Нажмите **Create**
3. Убедитесь, что поддомен указывает на правильную папку

### Шаг 3: Проверить права доступа

```bash
# Установить правильные права
chmod -R 755 ~/public_html/demo
chmod -R 644 ~/public_html/demo/*.html
```

### Шаг 4: Проверить работу

1. Откройте в браузере: `https://demo.rokets.delivery`
2. Должна открыться главная страница классифайда
3. Проверьте навигацию по страницам
4. Откройте консоль браузера (F12) - не должно быть ошибок загрузки ресурсов

## ⚠️ Важно о NEXT_PUBLIC_BASE_PATH

**Критически важно:** Если при сборке использовали `NEXT_PUBLIC_BASE_PATH=/demo`, то:
- Файлы должны быть в `public_html/demo/`
- URL должен быть `rokets.delivery/demo` (не поддомен!)

Если при сборке использовали `NEXT_PUBLIC_BASE_PATH=` (пусто), то:
- Файлы должны быть в корне поддомена
- URL должен быть `demo.rokets.delivery` (поддомен)

**Проверьте, какой `NEXT_PUBLIC_BASE_PATH` использовался при сборке!**

## 🔄 Обновление статической версии

Когда нужно обновить сайт:

### Локально:

```bash
cd C:\Dev\CursorProjects\Rokets_clsfd\commerce

# Пересобрать проект
npm run build

# Добавить обновленную папку out/ в Git
git add out/
git commit -m "Update static export"
git push origin main
```

### На Verpex:

```bash
# Обновить репозиторий
cd ~
rm -rf rokets-temp
git clone https://github.com/ShavalievER/rokets-classifieds.git rokets-temp

# Скопировать новые файлы
cp -r ~/rokets-temp/commerce/out/* ~/public_html/demo/
cp -r ~/rokets-temp/commerce/out/.* ~/public_html/demo/ 2>/dev/null || true

# Удалить временную папку
rm -rf ~/rokets-temp
```

**Ничего перезапускать не нужно!** Статические файлы работают сразу.

## 🛠️ Устранение проблем

### Страница не открывается (404)

**Решение:**
1. Проверьте, что файлы скопированы: `ls -la ~/public_html/demo/`
2. Проверьте настройки поддомена в cPanel → **Subdomains**
3. Убедитесь, что Document Root указывает на `public_html/demo`

### Страница открывается, но белая (без стилей)

**Решение:**
1. Откройте консоль браузера (F12) → вкладка Console
2. Проверьте ошибки загрузки ресурсов
3. **Скорее всего проблема с `NEXT_PUBLIC_BASE_PATH`!**
   - Если видите ошибки типа `/_next/static/...` → пути не совпадают
   - Проверьте, какой `NEXT_PUBLIC_BASE_PATH` использовался при сборке
   - Убедитесь, что путь на сервере совпадает с путем при сборке

### Ошибка 403 Forbidden

**Решение:**
```bash
chmod -R 755 ~/public_html/demo
chmod -R 644 ~/public_html/demo/*.html
```

### Файлы не обновляются

**Решение:**
1. Убедитесь, что выполнили `git pull` или клонировали заново
2. Проверьте, что скопировали файлы из `out/` в `public_html/demo/`
3. Очистите кеш браузера (Ctrl+F5)

## 📝 Создание скрипта для автоматического обновления

Создайте скрипт для упрощения обновления:

```bash
cat > ~/update-demo.sh << 'EOF'
#!/bin/bash
echo "Updating demo.rokets.delivery..."

# Обновить репозиторий
cd ~
rm -rf rokets-temp
git clone https://github.com/ShavalievER/rokets-classifieds.git rokets-temp

# Скопировать новые файлы
cp -r ~/rokets-temp/commerce/out/* ~/public_html/demo/
cp -r ~/rokets-temp/commerce/out/.* ~/public_html/demo/ 2>/dev/null || true

# Удалить временную папку
rm -rf ~/rokets-temp

echo "✅ Demo site updated successfully"
EOF

chmod +x ~/update-demo.sh
```

Использование:
```bash
~/update-demo.sh
```

## ✅ Готово!

После выполнения всех шагов сайт должен работать на `https://demo.rokets.delivery` без Node.js App и без проблем с памятью!

## 📚 Дополнительная информация

- Подробная инструкция: [VERPEX_STATIC_GIT_DEPLOY.md](./VERPEX_STATIC_GIT_DEPLOY.md)
- Быстрый старт: [QUICK_START_STATIC.md](./QUICK_START_STATIC.md)

