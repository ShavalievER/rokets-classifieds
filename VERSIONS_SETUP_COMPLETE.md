# ✅ Настройка версий завершена!

## 🎉 Что сделано

1. ✅ **Статичная версия** сохранена в ветке `main`
   - Работает на `demo.rokets.delivery` без Node.js App
   - Папка `out/` в Git для развертывания

2. ✅ **Нестатичная версия** сохранена в ветке `dynamic`
   - Готова для полноценной работы с Node.js
   - `next.config.ts` без `output: 'export'`
   - `server.js` - полноценный Next.js сервер

3. ✅ **Синхронизация настроена**
   - Скрипты для синхронизации созданы
   - Инструкции по работе готовы

## 📚 Документация

- **SYNC_GUIDE.md** - простое руководство по синхронизации
- **WORKFLOW_SYNC.md** - подробный рабочий процесс
- **SAVE_DYNAMIC_VERSION.md** - настройка нестатичной версии
- **SERVER_VERSIONS.md** - описание версий серверов

## 🚀 Быстрый старт

### Работа с кодом (app/, components/, lib/)

**Рекомендуемый процесс:**

1. **Работайте в main** (статичная версия)
2. **Вносите изменения** в код
3. **Коммитьте и пушите:**
   ```bash
   git add .
   git commit -m "Update: ..."
   git push origin main
   ```

4. **Синхронизируйте в dynamic:**
   ```bash
   git checkout dynamic
   git checkout main -- app/ components/ lib/ public/ package.json tsconfig.json
   git add .
   git commit -m "Sync: Update from main"
   git push origin dynamic
   git checkout main
   ```

### Развертывание

- **Статичная версия (main):** Развертывается на `demo.rokets.delivery` через веб-сервер
- **Нестатичная версия (dynamic):** Развертывается через Node.js App на Verpex

## 📋 Структура веток

```
main (статичная)
├── next.config.ts (может иметь output: 'export')
├── server.js (полноценный Next.js сервер)
├── out/ (статический экспорт в Git)
└── app/, components/, lib/ (код)

dynamic (нестатичная)
├── next.config.ts (БЕЗ output: 'export')
├── server.js (полноценный Next.js сервер)
├── .next/ (результат сборки, не в Git)
└── app/, components/, lib/ (код, синхронизируется с main)
```

## 🔄 Синхронизация изменений

### Что синхронизировать:
- ✅ `app/` - страницы
- ✅ `components/` - компоненты  
- ✅ `lib/` - библиотеки
- ✅ `public/` - статические файлы
- ✅ `package.json` - зависимости
- ✅ `tsconfig.json` - настройки TypeScript

### Что НЕ синхронизировать:
- ❌ `next.config.ts` - разные настройки
- ❌ `server.js` - разные версии (хотя сейчас одинаковые)
- ❌ `.next/` и `out/` - результаты сборки

## 💡 Советы

1. **Работайте в одной ветке** (main), синхронизируйте при необходимости
2. **Коммитьте перед синхронизацией** - так проще откатить изменения
3. **Тестируйте обе версии** после синхронизации
4. **Используйте понятные сообщения коммитов** - так проще отслеживать изменения

## 🎯 Следующие шаги

1. ✅ Статичная версия работает на `demo.rokets.delivery`
2. ✅ Нестатичная версия сохранена в Git
3. ✅ Синхронизация настроена
4. 🔄 При необходимости развернуть нестатичную версию на Verpex

## 📞 Если нужна помощь

- Синхронизация: см. `SYNC_GUIDE.md`
- Развертывание статики: см. `DEPLOY_STATIC_DEMO.md`
- Развертывание динамики: см. `VERPEX_SETUP.md`










