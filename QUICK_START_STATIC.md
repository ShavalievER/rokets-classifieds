# Быстрый запуск статической версии на Verpex

## Шаг 1: Проверьте наличие файлов

Убедитесь, что на сервере есть:
- ✅ Папка `out/` (со статическими файлами)
- ✅ Файл `server-static.js` (статический сервер)

## Шаг 2: Настройте Node.js App в cPanel

1. **cPanel → Setup Node.js App**
2. Найдите ваше приложение или создайте новое
3. **Application startup file**: `server-static.js`
4. **Application root**: путь к папке с `package.json` (например, `public_html/rokets-classifieds`)
5. **Node.js version**: `20.x` или `18.x`

## Шаг 3: Environment Variables (если нужно)

Если используете поддомен (demo.rokets.delivery):
```
PORT=3001
NEXT_PUBLIC_BASE_PATH=
```

Если используете путь (rokets.delivery/demo):
```
PORT=3001
NEXT_PUBLIC_BASE_PATH=/demo
```

## Шаг 4: Запустите

1. Нажмите **Restart** или **Run**
2. Откройте URL вашего приложения

## Готово! 🎉

Если что-то не работает, смотрите подробную инструкцию: [VERPEX_STATIC_DEPLOY.md](./VERPEX_STATIC_DEPLOY.md)

