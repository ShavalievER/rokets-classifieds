# Быстрый запуск статической версии на Verpex

## ⚠️ Важно: Статическая версия НЕ требует Node.js App!

Статическая версия работает через обычный веб-сервер (Apache/Nginx), который уже есть на Verpex.

## 🚀 Рекомендуемый способ: Через Git

См. подробную инструкцию: [VERPEX_STATIC_GIT_DEPLOY.md](./VERPEX_STATIC_GIT_DEPLOY.md)

## Альтернатива: Через ZIP архив

### Шаг 1: Подготовьте файлы

1. Создайте ZIP архив **содержимого** папки `out/` (не самой папки!)
2. В архиве должны быть: `index.html`, `404.html`, `_next/`, `account/`, `product/` и т.д.

## Шаг 2: Загрузите на Verpex

### Для поддомена (demo.rokets.delivery):

1. **cPanel → File Manager**
2. Перейдите в `public_html/demo/` (или `public_html/subdomains/demo/`)
3. Загрузите и распакуйте архив
4. Файлы должны быть напрямую в этой папке

### Для пути (rokets.delivery/demo):

1. **cPanel → File Manager**
2. Перейдите в `public_html/`
3. Создайте папку `demo/` (если нет)
4. Загрузите и распакуйте архив в `public_html/demo/`

## Шаг 3: Настройте поддомен (если нужно)

1. **cPanel → Subdomains**
2. Убедитесь, что поддомен `demo` указывает на правильную папку
3. Если поддомена нет - создайте его

## Шаг 4: Проверьте

1. Откройте `https://demo.rokets.delivery` (или `https://rokets.delivery/demo`)
2. Должна открыться главная страница

## ⚠️ Важно о путях!

Если при сборке использовали `NEXT_PUBLIC_BASE_PATH=/demo`, то:
- Файлы должны быть в `public_html/demo/`
- URL должен быть `rokets.delivery/demo`

Если при сборке использовали `NEXT_PUBLIC_BASE_PATH=` (пусто), то:
- Файлы должны быть в корне поддомена
- URL должен быть `demo.rokets.delivery`

## Готово! 🎉

**Ничего перезапускать не нужно** - статические файлы работают сразу!

Если что-то не работает, смотрите подробную инструкцию: [VERPEX_STATIC_DEPLOY.md](./VERPEX_STATIC_DEPLOY.md)

