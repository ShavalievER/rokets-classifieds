# Развертывание статической версии на Verpex

Эта инструкция описывает, как запустить статическую версию классифайда на Verpex.

## Что такое статическая версия?

Статическая версия - это полностью собранный сайт в папке `out/`, который не требует Node.js для работы. Все страницы предварительно сгенерированы как HTML файлы.

**Преимущества:**
- ✅ Быстрая загрузка
- ✅ Низкое потребление ресурсов
- ✅ Не требует сборки на сервере
- ✅ Работает на любом хостинге

**Недостатки:**
- ❌ Нет динамических API routes
- ❌ Нет серверного рендеринга
- ❌ Нужно пересобирать при каждом изменении

## Шаг 1: Подготовка статического экспорта

### Вариант A: Если у вас уже есть папка `out/`

Если папка `out/` уже существует (как в вашем случае), переходите к Шагу 2.

### Вариант B: Создать статический экспорт заново

1. Убедитесь, что в `next.config.ts` есть настройка:

```typescript
export default {
  output: 'export',  // Добавьте эту строку
  basePath: process.env.NEXT_PUBLIC_BASE_PATH || '',
  assetPrefix: process.env.NEXT_PUBLIC_BASE_PATH || '',
  // ... остальные настройки
};
```

2. Соберите проект локально:

```bash
cd commerce
npm install
npm run build
```

После сборки появится папка `out/` со всеми статическими файлами.

## Шаг 2: Загрузка на Verpex

### ⚡ Быстрый способ: Через Git (рекомендуется)

См. подробную инструкцию: [VERPEX_STATIC_GIT_DEPLOY.md](./VERPEX_STATIC_GIT_DEPLOY.md)

### Вариант A: Для поддомена (например, demo.rokets.delivery) - через ZIP

1. **Создайте ZIP архив** содержимого папки `out/`:
   - Откройте папку `out/`
   - Выберите ВСЕ файлы и папки внутри (не саму папку `out/`)
   - Создайте ZIP архив (например, `static-site.zip`)

2. **Загрузите на Verpex**:
   - Зайдите в **cPanel → File Manager**
   - Перейдите в папку поддомена (обычно `public_html/demo/` или `public_html/subdomains/demo/`)
   - Если папки нет, создайте её
   - Загрузите `static-site.zip`
   - Распакуйте архив (Extract)
   - **Важно:** Файлы должны быть напрямую в папке поддомена, не в подпапке `out/`

3. **Проверьте структуру**:
   ```
   public_html/demo/
   ├── index.html
   ├── 404.html
   ├── _next/
   ├── account/
   ├── product/
   └── ... (все файлы из out/)
   ```

### Вариант B: Для пути (например, rokets.delivery/demo)

1. **Создайте ZIP архив** содержимого папки `out/` (как в Варианте A)

2. **Загрузите на Verpex**:
   - Зайдите в **cPanel → File Manager**
   - Перейдите в `public_html/`
   - Создайте папку `demo/` (если её нет)
   - Загрузите и распакуйте архив в `public_html/demo/`

3. **Проверьте структуру**:
   ```
   public_html/demo/
   ├── index.html
   ├── 404.html
   ├── _next/
   └── ... (все файлы из out/)
   ```

### Вариант C: Через FTP

1. Подключитесь к серверу через FTP клиент (FileZilla, WinSCP)
2. Перейдите в нужную папку (для поддомена или пути)
3. Загрузите ВСЕ содержимое папки `out/` (не саму папку)

## Шаг 3: Настройка веб-сервера (БЕЗ Node.js App!)

**Важно:** Статическая версия НЕ требует Node.js App! Она работает через обычный веб-сервер (Apache/Nginx).

### Для поддомена:

1. **Проверьте настройки поддомена** в cPanel:
   - Зайдите в **Subdomains**
   - Убедитесь, что поддомен указывает на правильную папку
   - Обычно это `public_html/demo/` или `public_html/subdomains/demo/`

2. **Если нужно создать поддомен:**
   - **cPanel → Subdomains**
   - **Subdomain**: `demo`
   - **Document Root**: `public_html/demo` (или `public_html/subdomains/demo`)
   - Нажмите **Create**

### Для пути:

Ничего дополнительно настраивать не нужно - файлы уже в `public_html/demo/`

### Создание .htaccess (опционально, для Apache)

Если нужно настроить редиректы или обработку 404, создайте файл `.htaccess` в папке с файлами:

```apache
# Enable rewrite engine
RewriteEngine On

# Handle Next.js routing (SPA fallback)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /404.html [L]

# Cache static assets
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>
```

**Важно:** Если вы используете поддомен с `NEXT_PUBLIC_BASE_PATH=/demo`, то `.htaccess` должен быть настроен соответственно.

## Шаг 4: Проверка

1. Откройте URL вашего приложения:
   - Для поддомена: `https://demo.rokets.delivery`
   - Для пути: `https://rokets.delivery/demo`

2. Должна открыться главная страница классифайда

3. Проверьте навигацию по страницам

4. Проверьте загрузку стилей и изображений (откройте консоль браузера F12)

## Важно о NEXT_PUBLIC_BASE_PATH

**Критически важно:** Переменная `NEXT_PUBLIC_BASE_PATH`, которая использовалась при сборке, должна совпадать с путем на сервере!

- Если собирали с `NEXT_PUBLIC_BASE_PATH=/demo` → файлы должны быть в `public_html/demo/`
- Если собирали с `NEXT_PUBLIC_BASE_PATH=` (пусто) → файлы должны быть в корне поддомена `public_html/demo/` (или `public_html/subdomains/demo/`)

Если пути не совпадают, стили и скрипты не загрузятся!

## Устранение проблем

### Ошибка: "404 - File Not Found" или пустая страница

**Решение:**
1. Проверьте, что файлы загружены в правильную папку:
   - Для поддомена: `public_html/demo/` или `public_html/subdomains/demo/`
   - Для пути: `public_html/demo/`
2. Убедитесь, что файл `index.html` находится в корне папки
3. Проверьте настройки поддомена в cPanel → **Subdomains** (Document Root должен указывать на папку с файлами)

### Страница открывается, но стили не работают (белая страница без стилей)

**Решение:**
1. **Проверьте консоль браузера (F12)** - там будут ошибки загрузки ресурсов
2. **Проверьте пути к ресурсам:**
   - Если видите ошибки типа `/_next/static/...` → проблема с `NEXT_PUBLIC_BASE_PATH`
   - Если собирали с `/demo`, то пути должны быть `/demo/_next/static/...`
3. **Проверьте, что папка `_next/` загружена полностью**
4. **Убедитесь, что `NEXT_PUBLIC_BASE_PATH` при сборке совпадает с путем на сервере**

### Ошибка: "403 Forbidden"

**Решение:**
1. Проверьте права доступа к файлам в File Manager
2. Убедитесь, что файлы имеют права на чтение (обычно 644 для файлов, 755 для папок)

### Страница не открывается вообще

**Решение:**
1. Проверьте настройки поддомена в cPanel → **Subdomains**
2. Убедитесь, что DNS настроен правильно (если используете внешний DNS)
3. Проверьте, что файлы действительно загружены (откройте File Manager)
4. Попробуйте открыть напрямую: `https://demo.rokets.delivery/index.html`

## Обновление статической версии

Когда нужно обновить сайт:

1. **Локально:**
   ```bash
   npm run build
   ```

2. **Создайте ZIP** папки `out/`

3. **На Verpex:**
   - Загрузите новый архив
   - Распакуйте в папку поддомена/пути (замените старые файлы)
   - **Ничего перезапускать не нужно!** Статические файлы работают сразу

## Сравнение версий

| Характеристика | Статическая (без Node.js) | Динамическая (server.js) |
|---------------|---------------------------|--------------------------|
| Скорость | ⚡ Очень быстро | 🐢 Зависит от сервера |
| Память | 💚 Минимум (0 MB для Node.js) | 🔴 Высокое потребление |
| Node.js App | ❌ НЕ нужен | ✅ Обязателен |
| Обновление | 🔄 Нужна пересборка + загрузка файлов | ✅ Горячая перезагрузка |
| API Routes | ❌ Не работает | ✅ Работает |
| SSR | ❌ Нет | ✅ Есть |
| Стоимость ресурсов | 💚 Минимум | 🔴 Выше |

## Следующие шаги

После успешного запуска статической версии:
1. Проверьте все страницы сайта
2. Убедитесь, что навигация работает
3. Проверьте загрузку изображений и стилей
4. Сохраните бэкап нестатической версии в Git (как планировали)

