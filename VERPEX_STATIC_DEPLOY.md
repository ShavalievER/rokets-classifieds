# Развертывание статической версии на Verpex

Эта инструкция описывает, как запустить статическую версию классифайда на Verpex.

## Что такое статическая версия?

Статическая версия - это полностью собранный сайт в папке `out/`, который не требует Node.js для работы. Все страницы предварительно сгенерированы как HTML файлы.

**Преимущества:**
- ✅ Быстрая загрузка
- ✅ Низкое потребление ресурсов
- ✅ Не требует сборки на сервере
- ✅ Работает на любом хостинге

**Недостатки:**
- ❌ Нет динамических API routes
- ❌ Нет серверного рендеринга
- ❌ Нужно пересобирать при каждом изменении

## Шаг 1: Подготовка статического экспорта

### Вариант A: Если у вас уже есть папка `out/`

Если папка `out/` уже существует (как в вашем случае), переходите к Шагу 2.

### Вариант B: Создать статический экспорт заново

1. Убедитесь, что в `next.config.ts` есть настройка:

```typescript
export default {
  output: 'export',  // Добавьте эту строку
  basePath: process.env.NEXT_PUBLIC_BASE_PATH || '',
  assetPrefix: process.env.NEXT_PUBLIC_BASE_PATH || '',
  // ... остальные настройки
};
```

2. Соберите проект локально:

```bash
cd commerce
npm install
npm run build
```

После сборки появится папка `out/` со всеми статическими файлами.

## Шаг 2: Загрузка на Verpex

### Вариант A: Через File Manager (рекомендуется)

1. **Создайте ZIP архив** папки `out/`:
   - Выберите папку `out/`
   - Создайте ZIP архив (например, `out.zip`)

2. **Загрузите архив на Verpex**:
   - Зайдите в **cPanel → File Manager**
   - Перейдите в папку, где находится проект (например, `public_html/rokets-classifieds/`)
   - Загрузите `out.zip`
   - Распакуйте архив (Extract)
   - Убедитесь, что папка `out/` находится в корне проекта

3. **Загрузите `server-static.js`**:
   - Загрузите файл `server-static.js` в ту же папку, где находится `package.json`

### Вариант B: Через FTP

1. Подключитесь к серверу через FTP клиент (FileZilla, WinSCP)
2. Загрузите папку `out/` в корень проекта
3. Загрузите файл `server-static.js`

## Шаг 3: Настройка Node.js App в cPanel

1. Зайдите в **cPanel → Setup Node.js App**

2. **Если приложение уже создано:**
   - Нажмите **Edit** (карандаш)
   - Измените **Application startup file** на: `server-static.js`
   - Нажмите **Save**

3. **Если приложение не создано:**
   - Нажмите **Create Application**
   - **Application root**: `public_html/rokets-classifieds` (или ваш путь)
   - **Application startup file**: `server-static.js`
   - **Node.js version**: `20.x` (или 18.x)
   - Нажмите **Create**

4. **Настройте Environment Variables**:
   - Нажмите на ваше приложение → **Environment Variables**
   - Добавьте (если нужно):
     ```
     PORT=3001
     NEXT_PUBLIC_BASE_PATH=/demo
     ```

5. **Установите зависимости** (минимальные):
   - Нажмите **Run npm install**
   - Для статического сервера зависимости не нужны, но если есть ошибки, установите базовые пакеты

6. **НЕ запускайте сборку!** (папка `out/` уже готова)

7. **Запустите приложение**:
   - Нажмите **Restart** или **Run**

## Шаг 4: Проверка

1. Откройте URL вашего приложения (например, `https://demo.rokets.delivery`)
2. Должна открыться главная страница классифайда
3. Проверьте навигацию по страницам

## Шаг 5: Настройка домена/поддомена (если нужно)

Если вы используете поддомен (например, `demo.rokets.delivery`):
- В Environment Variables установите: `NEXT_PUBLIC_BASE_PATH=` (пусто)

Если вы используете путь (например, `rokets.delivery/demo`):
- В Environment Variables установите: `NEXT_PUBLIC_BASE_PATH=/demo`

**Важно:** Переменная `NEXT_PUBLIC_BASE_PATH` должна совпадать с той, что использовалась при сборке!

## Устранение проблем

### Ошибка: "out/ directory not found"

**Решение:**
1. Проверьте, что папка `out/` загружена на сервер
2. Проверьте путь в File Manager
3. Убедитесь, что папка находится в той же директории, что и `server-static.js`

### Ошибка: "404 - File Not Found"

**Решение:**
1. Проверьте, что `NEXT_PUBLIC_BASE_PATH` совпадает с настройками при сборке
2. Проверьте структуру папки `out/` - должны быть файлы `index.html`, `404.html` и т.д.

### Страница открывается, но стили не работают

**Решение:**
1. Проверьте, что папка `out/_next/` загружена полностью
2. Проверьте консоль браузера на ошибки загрузки ресурсов
3. Убедитесь, что `assetPrefix` в `next.config.ts` совпадает с `NEXT_PUBLIC_BASE_PATH`

### Порт занят или ошибка запуска

**Решение:**
1. Проверьте, что порт в Environment Variables совпадает с портом в настройках Node.js App
2. Проверьте логи в cPanel → **Setup Node.js App** → **View Logs**

## Обновление статической версии

Когда нужно обновить сайт:

1. **Локально:**
   ```bash
   npm run build
   ```

2. **Создайте ZIP** папки `out/`

3. **На Verpex:**
   - Загрузите новый `out.zip`
   - Распакуйте (замените старую папку `out/`)
   - Перезапустите приложение (Restart)

## Сравнение версий

| Характеристика | Статическая (server-static.js) | Динамическая (server.js) |
|---------------|-------------------------------|--------------------------|
| Скорость | ⚡ Очень быстро | 🐢 Зависит от сервера |
| Память | 💚 Минимум | 🔴 Высокое потребление |
| Обновление | 🔄 Нужна пересборка | ✅ Горячая перезагрузка |
| API Routes | ❌ Не работает | ✅ Работает |
| SSR | ❌ Нет | ✅ Есть |

## Следующие шаги

После успешного запуска статической версии:
1. Проверьте все страницы сайта
2. Убедитесь, что навигация работает
3. Проверьте загрузку изображений и стилей
4. Сохраните бэкап нестатической версии в Git (как планировали)

