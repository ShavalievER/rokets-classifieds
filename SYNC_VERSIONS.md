# Синхронизация статичной и нестатичной версий

## Структура проекта

Проект поддерживает две версии:
- **Статичная версия** (main branch) - для развертывания без Node.js
- **Нестатичная версия** (dynamic branch) - для полноценной работы с Node.js

## Настройка синхронизации

### Шаг 1: Создать ветку для нестатичной версии

```bash
cd C:\Dev\CursorProjects\Rokets_clsfd\commerce

# Создать новую ветку из текущей
git checkout -b dynamic

# Или если ветка уже существует
git checkout dynamic
```

### Шаг 2: Скопировать файлы из бэкапа

```bash
# Скопировать файлы из бэкапа в текущую папку
# (нужно будет вручную скопировать нужные файлы)
```

### Шаг 3: Настроить конфигурацию для нестатичной версии

В ветке `dynamic` нужно:
- Убрать `output: 'export'` из `next.config.ts` (если есть)
- Убедиться, что `server.js` - полноценный Next.js сервер
- Сохранить изменения

### Шаг 4: Создать скрипты синхронизации

Скрипты для синхронизации изменений между версиями.










