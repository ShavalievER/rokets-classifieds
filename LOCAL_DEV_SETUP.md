# Локальная разработка: статичная и нестатичная версии

## 🚀 Быстрый старт

### Статичная версия (порт 3000)

```bash
cd C:\Dev\CursorProjects\Rokets_clsfd\commerce
git checkout main
npm run test:static
```

Откроется на: `http://localhost:3000`

### Нестатичная версия (порт 3005)

```bash
cd C:\Dev\CursorProjects\Rokets_clsfd\commerce
git checkout dynamic
npm run serve:dynamic
```

Или напрямую:
```bash
node server-local.js
```

Откроется на: `http://localhost:3005`

## 📋 Доступные команды

### В ветке main (статичная):
- `npm run test:static` - запустить статичный сервер на порту 3000
- `npm run serve:static` - то же самое

### В ветке dynamic (нестатичная):
- `npm run serve:dynamic` - запустить нестатичный сервер на порту 3005
- `npm run dev:dynamic` - то же самое
- `npm run dev` - стандартный Next.js dev сервер (если нужен)

## 🔧 Настройки

### server-local.js (нестатичная версия)
- **Порт**: 3005 (можно изменить через `PORT=3005`)
- **Режим**: Development (`dev: true`)
- **Хост**: `localhost` (только локальный доступ)

### test-static-local.js (статичная версия)
- **Порт**: 3000
- **Режим**: Статические файлы из папки `out/`

## 💡 Советы

1. **Работайте в ветке dynamic** для разработки нестатичной версии
2. **Используйте Hot Reload** - изменения применяются автоматически
3. **Проверяйте консоль** - там будут ошибки и логи
4. **После изменений синхронизируйте** с другой версией (см. SYNC_GUIDE.md)

## 🛑 Остановка серверов

Нажмите `Ctrl+C` в терминале, где запущен сервер.

## 🔍 Отладка

### Проверка работы сервера

```bash
# Проверить, что порт занят
netstat -ano | findstr :3005

# Или через PowerShell
Get-NetTCPConnection -LocalPort 3005 -ErrorAction SilentlyContinue
```

### Логи

Все логи выводятся в консоль. Для сохранения в файл:

```bash
npm run serve:dynamic > server.log 2>&1
```

## ⚠️ Важно

- **Статичная версия** (3000) - только чтение, изменения не применяются
- **Нестатичная версия** (3005) - полная разработка с Hot Reload
- **Переключайтесь между ветками** для работы с разными версиями










