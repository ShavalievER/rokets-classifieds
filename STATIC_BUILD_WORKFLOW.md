# Рабочий процесс обновления статичной версии

## 🎯 Важно

**Папка `out/` всегда должна быть актуальной в Git!**

При любых изменениях в статичной версии (main branch) нужно:
1. Пересобрать проект (`npm run build`)
2. Добавить обновленную папку `out/` в Git
3. Закоммитить и отправить изменения

## 📋 Рабочий процесс

### Вариант 1: Использовать скрипт (рекомендуется)

```powershell
# После изменений в коде
cd C:\Dev\CursorProjects\Rokets_clsfd\commerce
git checkout main

# Внесли изменения в app/, components/, lib/ и т.д.
# ...

# Обновить статичный билд и закоммитить
.\update-static-build.ps1

# Отправить в репозиторий
git push origin main
```

### Вариант 2: Вручную

```bash
# 1. Пересобрать проект
npm run build

# 2. Добавить out/ в Git
git add out/

# 3. Проверить изменения
git status --short out/

# 4. Закоммитить
git commit -m "Update: Rebuild static export (out/)"

# 5. Отправить
git push origin main
```

### Вариант 3: Через npm скрипт

```bash
# Собрать и автоматически закоммитить (если есть изменения)
npm run build:static

# Или только собрать и проверить
npm run update:static
```

## ⚠️ Важные правила

### ✅ Всегда обновляйте out/ когда:

- Изменили код в `app/`, `components/`, `lib/`
- Изменили `package.json` (зависимости)
- Изменили `public/` (статические файлы)
- Изменили конфигурацию, влияющую на сборку

### ❌ Не нужно обновлять out/ когда:

- Изменили только документацию (`.md` файлы)
- Изменили только скрипты сборки
- Изменили только `.gitignore`

## 🔄 Полный рабочий процесс

### 1. Внесли изменения в код

```bash
git checkout main
# ... редактируете файлы ...
git add app/ components/ lib/
git commit -m "Update: ..."
```

### 2. Пересобрали и обновили out/

```powershell
# Используйте скрипт
.\update-static-build.ps1

# Или вручную
npm run build
git add out/
git commit -m "Update: Rebuild static export"
```

### 3. Отправили все изменения

```bash
git push origin main
```

### 4. Синхронизировали с dynamic (если нужно)

```bash
git checkout dynamic
git checkout main -- app/ components/ lib/ public/ package.json tsconfig.json
git add .
git commit -m "Sync: Update from main"
git push origin dynamic
git checkout main
```

## 📝 Примеры коммитов

```bash
# Изменения в коде + обновление билда
git commit -m "Update: Add new feature"
.\update-static-build.ps1
git commit -m "Update: Rebuild static export"

# Или одним коммитом (если билд изменился)
git add app/ components/ out/
git commit -m "Update: Add new feature and rebuild static export"
```

## 🔍 Проверка

После обновления проверьте:

```bash
# Проверить, что out/ добавлен в Git
git ls-files | grep "^out/"

# Проверить размер папки out/
# Локально:
Get-ChildItem -Path out -Recurse | Measure-Object -Property Length -Sum

# Проверить последний коммит
git log --oneline -5
```

## 🚨 Если забыли обновить out/

Если отправили изменения в main без обновления out/:

```bash
# Пересобрать
npm run build

# Добавить и закоммитить
git add out/
git commit -m "Update: Rebuild static export (forgot in previous commit)"
git push origin main
```

## 💡 Автоматизация (опционально)

Можно настроить Git hook для автоматического обновления out/ при коммите, но это может замедлить коммиты. Рекомендуется использовать скрипт вручную.

## 📚 Связанные документы

- **SYNC_GUIDE.md** - синхронизация между версиями
- **WORKFLOW_SYNC.md** - подробный рабочий процесс
- **LOCAL_DEV_SETUP.md** - локальная разработка










